import type { NextFunction, Request, Response } from 'express';
import { SanitizeOptions } from 'xss-shield';
/**
 * Sanitizes user input from potential cross-site scripting (XSS) attacks.
 *
 * @param options - Optional configuration options to customize the sanitization process.
 *
 */
declare function xssShield(options?: SanitizeOptions): (req: Request, _res: Response, next: NextFunction) => void;
export default xssShield;
